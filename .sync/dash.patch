--- a/dashboard.yaml
+++ b/dashboard.yaml
@@ -215,9 +215,11 @@
                     const forecastBias = (_scheduleBias && _scheduleBias !== 'unknown' && _scheduleBias !== 'unavailable')
                       ? parseFloat(_scheduleBias) / 100
                       : _cardBias / 100;
-                    const pvAvailabilityFactor = Math.min(Math.max(
-
-                      parseFloat(hass.states['sensor.youems_pv_availability']?.state || 100) / 100, 0), 1);
+                    const pvAvailabilityFactor =
+                      hass.states['input_boolean.youems_pv_forecast_adaptation_enabled']?.state === 'on'
+                        ? Math.min(Math.max(
+                            parseFloat(hass.states['sensor.youems_pv_availability']?.state || 100) / 100, 0), 1)
+                        : 1;
 
                     const drainFlat = avgDaily / 24; // raw kW fallback; forecast bias is applied once in getBandKw
                     // Band drain kW — delta between band boundaries, 2-day average
@@ -452,9 +454,11 @@
                     const forecastBias = (_scheduleBias && _scheduleBias !== 'unknown' && _scheduleBias !== 'unavailable')
                       ? parseFloat(_scheduleBias) / 100
                       : _cardBias / 100;
-                    const pvAvailabilityFactor = Math.min(Math.max(
-
-                      parseFloat(hass.states['sensor.youems_pv_availability']?.state || 100) / 100, 0), 1);
+                    const pvAvailabilityFactor =
+                      hass.states['input_boolean.youems_pv_forecast_adaptation_enabled']?.state === 'on'
+                        ? Math.min(Math.max(
+                            parseFloat(hass.states['sensor.youems_pv_availability']?.state || 100) / 100, 0), 1)
+                        : 1;
 
                     const drainFlat = avgDaily / 24; // raw kW fallback; forecast bias is applied once in getBandKw
                     // Band drain kW — delta between band boundaries, 2-day average
@@ -674,9 +678,11 @@
                     const forecastBias = (_scheduleBias && _scheduleBias !== 'unknown' && _scheduleBias !== 'unavailable')
                       ? parseFloat(_scheduleBias) / 100
                       : _cardBias / 100;
-                    const pvAvailabilityFactor = Math.min(Math.max(
-
-                      parseFloat(hass.states['sensor.youems_pv_availability']?.state || 100) / 100, 0), 1);
+                    const pvAvailabilityFactor =
+                      hass.states['input_boolean.youems_pv_forecast_adaptation_enabled']?.state === 'on'
+                        ? Math.min(Math.max(
+                            parseFloat(hass.states['sensor.youems_pv_availability']?.state || 100) / 100, 0), 1)
+                        : 1;
 
                     const drainFlat = avgDaily / 24; // raw kW fallback; forecast bias is applied once in getBandKw
                     const snapVal = (key) => parseFloat(hass.states['input_number.house_snapshot_' + key]?.state || 0);
@@ -2086,42 +2092,18 @@
               - entity: input_boolean.youems_pv_forecast_adaptation_enabled
                 name: PV Forecast Adaptation
                 icon: mdi:solar-panel
-              - type: conditional
-                conditions:
-                  - condition: state
-                    entity: input_boolean.youems_pv_forecast_adaptation_enabled
-                    state: "on"
-                row:
-                  entity: input_number.youems_pv_shortfall_response_pct
-                  name: PV Shortfall Response
-              - type: conditional
-                conditions:
-                  - condition: state
-                    entity: input_boolean.youems_pv_forecast_adaptation_enabled
-                    state: "on"
-                row:
-                  entity: input_number.youems_pv_recovery_response_pct
-                  name: PV Recovery Response
-              - type: conditional
-                conditions:
-                  - condition: state
-                    entity: input_boolean.youems_pv_forecast_adaptation_enabled
-                    state: "on"
-                row:
-                  entity: sensor.youems_pv_availability
-                  tap_action:
-                    action: none
-                  name: PV Availability
-              - type: conditional
-                conditions:
-                  - condition: state
-                    entity: input_boolean.youems_pv_forecast_adaptation_enabled
-                    state: "on"
-                row:
-                  entity: sensor.youems_pv_availability_status
-                  tap_action:
-                    action: none
-                  name: PV Availability Status
+              - entity: input_number.youems_pv_shortfall_response_pct
+                name: PV Shortfall Response
+              - entity: input_number.youems_pv_recovery_response_pct
+                name: PV Recovery Response
+              - entity: sensor.youems_pv_availability
+                tap_action:
+                  action: none
+                name: PV Availability (learned)
+              - entity: sensor.youems_pv_availability_status
+                tap_action:
+                  action: none
+                name: PV Availability Status
               - entity: input_button.youems_shadow_soc_resync
                 name: Re-sync Shadow SOC Now
                 icon: mdi:battery-sync-outline
